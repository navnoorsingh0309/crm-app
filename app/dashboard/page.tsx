'use client'

import { SignOutButton, useClerk } from '@clerk/nextjs';

const Dashboard = () => {
  const { signOut } = useClerk();

  return (
    <div>
      
    </div>
  );
};

export default Dashboard;